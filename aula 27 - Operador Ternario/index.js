/*

(condição) ? 'valor verdadeiro' : 'valor falso';

? :
? ==> if
: ==> else

if (usu >= 1000){
    console.log('usuario Vip');
}
else{
    consolo.log('Usuario Normal');
}

const nivelUsuario = usu >=1000 ? 'Usuario Vip' : 'Usuario Normal';

const corUsuario = null;
cosnt corPadrao = corUsuario || 'Preta';

*/